// URL base de la aplicación
export const BASE_URL = 'https://practica.testqacademy.com';

// Rutas específicas
export const ROUTES = {
    STORE: '/store'
};

// URLs completas
export const PAGES = {
    STORE: `${BASE_URL}${ROUTES.STORE}`
};